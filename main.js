'use strict'

let currentValue = 0;

function add (val) {
    return currentValue += val;
}

function subtract (val) {
    return currentValue -= val;
}

function multiply (val) {
    return currentValue *= val;
}

function divide (val) {
    return currentValue /= val;
}

